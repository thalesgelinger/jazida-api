version: "2"
sql:
  - engine: "sqlite"
    queries: "sqlc/queries/*.sql"
    schema: "sqlc/schema.sql"
    gen:
      go:
        package: "db"
        out: "internal/infra/db"
        emit_json_tags: true
